

/**
 * la inicialización del cliente de la BD se realiza 
 * en el directorio "prisma/" 
 * en este mismo se definen los schemas y migraciones 
 * por lo tanto el cliente de psql se toma directamente 
 * desde "@prisma/client"
 */